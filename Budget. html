<!DOCTYPE html>
<html>
<head>
  <title>Budget Calculator - SmartDaily Tools</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      text-align: center;
    }
    .branding {
      background-color: #0A3D62;
      color: white;
      padding: 10px 20px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .branding img {
      height: 30px;
      margin-right: 10px;
    }
    h2 {
      margin-top: 20px;
    }
    input, button {
      margin: 8px;
      padding: 10px;
      font-size: 16px;
      border-radius: 5px;
      width: 80%;
      max-width: 300px;
    }
    button {
      background-color: #0A3D62;
      color: white;
      border: none;
    }
    .footer {
      margin-top: 50px;
      background-color: #f0f0f0;
      padding: 15px;
      font-size: 14px;
      color: #333;
    }
    .back-button {
      margin-top: 30px;
    }
  </style>
</head>
<body>

<!-- Branding Bar -->
<div class="branding">
  <img src="logo.png" alt="Logo" />
  SmartDaily Tools 🔧 | Free & Simple Tools for Everyday Use
</div>

<h2>Monthly Budget Calculator</h2>
<input type="number" id="income" placeholder="Total Income" /><br />
<input type="number" id="food" placeholder="Food & Groceries" /><br />
<input type="number" id="rent" placeholder="Rent or Housing" /><br />
<input type="number" id="bills" placeholder="Utilities & Bills" /><br />
<input type="number" id="transport" placeholder="Transport" /><br />
<input type="number" id="savings" placeholder="Savings" /><br />
<button onclick="calculateBudget()">Calculate</button>

<p id="result"></p>

<!-- Back to Home -->
<div class="back-button">
  <a href="index.html" style="text-decoration:none;">
    <button>⬅️ Back to Home</button>
  </a>
</div>

<!-- Footer -->
<div class="footer">
  © 2025 SmartDaily Tools. All rights reserved.
</div>

<script>
  function calculateBudget() {
    const income = parseFloat(document.getElementById('income').value) || 0;
    const food = parseFloat(document.getElementById('food').value) || 0;
    const rent = parseFloat(document.getElementById('rent').value) || 0;
    const bills = parseFloat(document.getElementById('bills').value) || 0;
    const transport = parseFloat(document.getElementById('transport').value) || 0;
    const savings = parseFloat(document.getElementById('savings').value) || 0;

    const totalExpenses = food + rent + bills + transport + savings;
    const balance = income - totalExpenses;

    let message = `🧾 Total Expenses: ${totalExpenses.toFixed(2)}<br>💵 Balance: ${balance.toFixed(2)}<br>`;
    if (balance < 0) {
      message += "⚠️ You're overspending!";
    } else {
      message += "✅ Great job staying within budget!";
    }

    document.getElementById('result').innerHTML = message;
  }
</script>

</body>
</html>
